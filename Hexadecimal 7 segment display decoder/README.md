This project implements a combinational logic decoder in SystemVerilog that converts a 4-bit hexadecimal input into the corresponding 7-segment display patterns. It supports digits 0-9 and letters A-F
The module is designed for a Common Cathode display configuration (where a logic 1 illuminates the segment).
